var app = angular.module("lookingForGroup"); //this will go retreive the module rather than create it
app.controller("query_controller", queryController); //declare new controller, and the function to call

function queryController(){
  //query attributes
  var vm = this;

  //query functions

}
